# WSPORT

## Обзор проекта
- **Название**: WSPORT (бренд на экране входа обновлён с «ФОРМА» на «WSPORT»)
- **Цель**: фитнес-сайт с расчётом нормы КБЖУ, подбором рецептов и упражнениями для зала
- **Стек**: чистый HTML/CSS/vanilla JS SPA, статика на Cloudflare Pages (worker `47733657-aa80-42b6-861e-c5b1d3a78291`)

## Что сделано в этой сессии
1. **Экран входа**: название заменено на «WSPORT»; текст «Аккаунт хранится только в этом браузере…» заменён на короткое, аккуратное описание сайта.
2. **Расчёт КБЖУ**: `calcNorm()` в `app.js` использует формулу Миффлина-Сан Жеора (BMR) и рекомендации ISSN Position Stand по белку (1.6–2.2 г/кг) и жирам (мин. 0.6 г/кг) — с комментариями в коде, объясняющими научное обоснование.
3. **Анимация подсветки БЖУ**: `.macro`/`macropop` в `styles.css` смягчена — длительность увеличена с 0.42s до 1.1s, ослаблен scale, добавлен плавный box-shadow переход вместо резкого мигания.
4. **Фото рецептов**: все 48 рецептов (36 ранее без фото + 12 уже имевших) теперь содержат `img` в `data.js`, файлы лежат в `public/static/img/food/`.
5. **Кнопка «Другой вариант»** удалена — она дублировала функцию «Собрать день».
6. **Анимация в разделе рецептов**: добавлен shimmer-блик по фото карточки при наведении (`.rphoto::before` + `@keyframes rshine`) и лёгкое появление бейджа калорий (`@keyframes rtagpop`), чтобы раздел не выглядел пустым.
7. **Бренд WSPORT везде**: заменены остававшиеся упоминания «ФОРМА» — `<title>`, бренд онбординга, бренд в сайдбаре приложения, бренд в верхней панели (topbar).
8. **Toast-уведомления при входе/регистрации**: после успешного входа снизу экрана показывается «Успешный вход!», после успешной регистрации — «Успешная регистрация!» (использован уже существовавший в проекте компонент `#toast`, просто добавлены вызовы в нужных местах).
9. **Ручной ввод КБЖУ**: на вкладке «Норма КБЖУ» добавлен переключатель «Рассчитать по параметрам» / «Указать свои цифры». В режиме ручного ввода пользователь задаёт калории, белки и жиры сам (углеводы досчитываются автоматически по остатку калорий). Введённые значения проверяются и мягко корректируются:
   - калории ограничены диапазоном 800–6000 ккал;
   - белки — 20–400 г, жиры — 20–300 г;
   - если известен вес пользователя, белок дополнительно ограничен ~3 г/кг, жиры ~2 г/кг (ISSN-рекомендации);
   - если белки+жиры «съедают» больше 85% калорий, они пропорционально уменьшаются, чтобы осталось место для углеводов;
   - под полями выводится понятное объяснение, что и почему было скорректировано.
   Выбранный режим (авто/ручной) и введённые значения сохраняются в профиле пользователя (localStorage) при нажатии «Сохранить в профиль».
10. **Расширена база упражнений (зал)**: было 26, добавлено 19 новых — теперь **44 упражнения** во всех группах. Каждое новое упражнение содержит фото, целевые/вторичные мышцы (в т.ч. коды для анатомической SVG-схемы), рекомендованные подходы/повторы, шаги техники и частую ошибку — по той же схеме, что и старые записи. Разбивка по группам:
    - **Грудь** (4 → 8): + сведение в кроссовере, пек-дек, жим на скамье с наклоном вниз, отжимания с узкой постановкой рук (алмазные)
    - **Спина** (4 → 7): + тяга Т-штанги, шраги со штангой, классическая тяга штанги (дедлифт)
    - **Ноги** (6 → 9): + разгибание ног в тренажёре, болгарские выпады, подъём на носки сидя
    - **Плечи** (3 → 5): + тяга штанги к подбородку, сведение на заднюю дельту в тренажёре
    - **Руки** (4 → 7): + французский жим лёжа, подъём на бицепс на скамье Скотта, концентрированный подъём на бицепс
    - **Пресс** (3 → 5): + велосипед (скручивания с поворотом), ролик для пресса (ab wheel)
    - **Ягодицы** (1 → 3): + отведение ноги назад в кроссовере, мост на одной ноге

## URLs
- **Production**: https://47733657-aa80-42b6-861e-c5b1d3a78291.vip.gensparksite.com
- **GitHub**: репозиторий проекта (см. настройки деплоя)

## Данные
- **Рецепты**: `public/static/data.js` → `RECIPES[]` (48 объектов: id, name, meal[], kind, time, kcal, p, f, c, img, ing[], steps[], hint)
- **Упражнения**: там же → `EXERCISES[]`
- **Хранение пользовательских данных**: localStorage браузера (без сервера)

## Как пользоваться
1. Зарегистрироваться/войти на экране входа (WSPORT) — после успешного действия внизу экрана появится уведомление.
2. На вкладке «Норма КБЖУ» выбрать способ: рассчитать по параметрам (пол, цель, возраст, рост, вес, активность) или указать свои цифры калорий/БЖУ вручную — сервис проверит их на адекватность и подскажет, если что-то нужно скорректировать.
3. На вкладке рецептов выбрать «Готовый день» → «Собрать день» — меню подберётся под цель.
4. Каждый рецепт имеет фото, время готовки, калории и БЖУ.

## Деплой
- **Платформа**: Cloudflare Pages / Genspark hosted deploy
- **Статус**: изменения закоммичены и подготовлены к деплою
- **Последнее обновление**: 2026-08-30

## Обновление: профиль, достижения, протеиновые коктейли
- **Хостинг**: GitHub Pages — https://maks00716006.github.io/Sport-site/ (деплой автоматически при пуше в `main`).
- **Профиль**: кнопка в левом нижнем углу (`#pfab`), выезжающая панель (`#pdrawer`): стрик записи еды, активность, приготовленные рецепты, избранное и лучшие по оценке.
- **Стрик еды**: `FoodStreak` в `app.js` — сейчас демо-данные. Когда появится дневник питания: `FoodStreak.setDays([...даты 'YYYY-MM-DD'])`.
- **Достижения**: 100 штук в `public/static/achievements.js` (`ACHIEVEMENTS`, 8 категорий). Статус считается из `achStats()` в `app.js`, полученные сохраняются в профиле.
- **Избранное**: сердечко на карточках рецептов (`profile.favs`).
- **Протеиновые коктейли**: вкладка в «Рецептах», 10 рецептов на протеине без вкуса (`SHAKES` в `data.js`), иллюстрации в `public/static/img/shakes/`.
- **Фото**: 3 фото упражнений заменены на референсные (incline, pecdeck, diamondpush); все фото еды приведены к 4:3 1200×900 и единой цветокоррекции.

## Обновление: Дневник, профиль
- **Меню**: Норма КБЖУ · Дневник · Рецепты · Упражнения. «История блюд» и «Мой вес» перенесены в профиль сворачиваемыми разделами (аккордеоны), «Достижения» тоже сворачиваются.
- **Дневник** (`public/static/diary.js`): неделя Пн–Вс со стриком записи еды, кольцо калорий «Потреблено X / норма» и «Осталось N ккал», Б/Ж/У к норме, карточки Завтрак/Обед/Ужин/Перекус с «+», Тренировка, Трекер воды (цель ≈ 30 мл × кг веса). Данные — `profile.diary[дата]`.
- **Добавление еды**: 1) поиск с мгновенными подсказками по встроенной базе (`foods.js`, 125 продуктов на 100 г) и рецептам сайта; 2) магазинные продукты и штрих-коды — OpenFoodFacts (`lc=ru&cc=ru`, российские товары выше, проверка контрольной цифры EAN). Сканер: встроенный `BarcodeDetector`, иначе ZXing с CDN (iPhone); 3) распознавание по фото — `VisionAI` (OpenAI с личным ключом пользователя или свой сервер-прокси). Лимиты OFF: 15 запросов/мин на товар, 10/мин на поиск — поэтому поиск по магазинным товарам по кнопке/Enter, с кэшем.
- **Тренировки**: упражнение (подсказки из базы сайта и недавних), подходы с повторами и весом, «прошлый раз» для сравнения, вес тела за день, «Мой прогресс» — графики веса тела и рабочих весов (+ оценка разового максимума по формуле Эпли).
- **Стрик** в профиле и достижения «Стрик питания» теперь считаются по реальным записям дневника.

## Обновление: поиск и точный «Готовый день»
- Из упражнений убраны «Жим штанги на скамье с наклоном вниз» и «Мост на одной ноге» — теперь **42 упражнения**.
- **Поиск** в «Рецептах» (по названию, ингредиентам, типу блюда; работает и для протеиновых коктейлей) и в «Упражнениях» (по названию, группе и мышцам; «бицепс» не путается с «бицепсом бедра»).
- **Готовый день** (`planDay` в `app.js`): подбирает блюда и размер порций (½–2 с шагом ¼) так, чтобы сумма совпала с нормой калорий (обычно ±0–15 ккал), с учётом нормы белка; при очень высокой норме добавляется второй перекус. Кнопка «В дневник на сегодня» записывает меню в дневник.

## Обновление: единая фотосессия блюд
- Все 58 фото (48 рецептов + 10 протеиновых коктейлей) заменены на единую серию: ракурс ~45°, тёмная столешница, льняная салфетка, тёплый свет из окна, 1200×896 (4:3). Файлы: `public/static/img/food/*.jpg`, `public/static/img/shakes/*.jpg`; старые фото и SVG-иллюстрации коктейлей удалены.
- Цифры и счётчики (норма ккал, кольцо калорий, КБЖУ, стрик, KPI) и кнопки больше не выделяются при двойном клике/протягивании; поля ввода выделяются как обычно.

## Обновление: всплывающие цитаты
- `public/static/quotesData.js` — 92 реальные цитаты (из интервью, речей, книг) на русском: спортсмены, стримеры и ютуберы, рэперы и артисты (в т.ч. MAYOT и OG Buda). Формат `{ id, text, author, category }`. Строки из песен не используются (авторские права).
- `public/static/quotes.js` — `QuoteToasts`: очередь без повторов (Фишер–Йетс, новый круг только после показа всех, стык кругов без повтора, очередь хранится в браузере), первая цитата через 12–20 с после самого первого входа, дальше раз в 15–25 минут на компьютере и раз в 30–45 минут на телефоне (только когда пользователь 15 с ничего не делает); на телефоне — компактная строка над нижним меню, закрывается тапом или свайпом (время следующей хранится в браузере, перезагрузка не сбрасывает паузу), показ 5,5 с (пауза при наведении), закрытие крестиком. Не показывается поверх окон, профиля, другого тоста и в фоновой вкладке. Настройки — `QuoteToasts.config`.
- Позиция: справа внизу на компьютере, над нижним меню на телефоне (не перекрывает кнопку профиля). Подложка `pointer-events:none`. Выключатель «Мотивирующие цитаты» — в профиле.

## Обновление: сканер v2, ИИ-ментор, «Запомнить меня», мобильная версия
- **Сканер штрих-кодов** (`diary.js`): встроенный `BarcodeDetector` (Android) → WASM-полифилл на zxing-cpp (`barcode-detector` с CDN, iPhone/ПК) → запасной ZXing JS. Задняя основная камера (не ultra-wide), 1080p, непрерывный автофокус, лёгкий зум, фонарик; распознаётся только зона под рамкой, ~10 кадров/с, два одинаковых чтения + контрольная цифра EAN. Подсказки: «Идёт поиск…», «Подноси ближе», «Слишком темно». Товара нет в базе / нет сети → уведомление «Товар не найден, попробуйте ввести вручную» и переход в текстовый поиск (с кнопкой «добавить свой с этим штрих-кодом»).
- **ИИ-ментор** (`static/chat.js`, промпт `static/chatPrompt.mjs`, сервер `worker/index.js`): кнопка справа внизу, окно с пузырями, стриминг ответа, история по аккаунту. Сервер `POST /api/chat` — прокси к Anthropic с ключом в секретах Cloudflare, системным промптом на сервере, проверкой домена, лимитом 20 запросов/мин.
  - Деплой сервера: `npm i` → `npx wrangler login` → `npx wrangler secret put ANTHROPIC_API_KEY` → `npx wrangler deploy`. Затем адрес `https://forma-app.<поддомен>.workers.dev/api/chat` вписать в `public/static/config.js` (для GitHub Pages). Сайт также работает прямо с адреса воркера.
- **Запомнить меня**: токен сессии — в localStorage на 30 дней (продлевается) или в sessionStorage до закрытия вкладки; авто-вход по действующему токену; пароли — SHA-256 с солью (старые аккаунты переводятся при входе).
- **Скорость**: мгновенный отклик кнопок (спиннер + блокировка), ленивый рендер вкладок, миниатюры 640px для карточек (`static/img/t/`, 3,8 МБ вместо 13,3), без тяжёлых размытий и бесконечных анимаций на телефоне, `content-visibility` для карточек.
- **Мобильная версия**: исправлен вылет контейнера за экран на 320px, все кнопки/поля ≥44×44, поля 16px (iOS не зумит), нижнее меню и плавающие кнопки прячутся при открытой клавиатуре, безопасные отступы.

## ИИ-ментор бесплатно (Puter.js)
- По умолчанию чат работает через **Puter.js** (`chatProvider: 'puter'` в `public/static/config.js`): без ключей, без сервера и без оплаты со стороны сайта. Посетитель один раз входит в бесплатный аккаунт Puter (всплывающее окно); если когда-нибудь исчерпает бесплатный лимит Puter — пополняет свой аккаунт сам.
- Системный промпт ментора тот же (`static/chatPrompt.mjs`). История, стриминг и оформление не изменились.
- Настройки чата (шестерёнка, «Бесплатно / Свой сервер / Свой ключ») **убраны** — посетитель ничего не настраивает. Режим задаёт только владелец в `static/config.js`; старые настройки из браузера игнорируются, поэтому статус «не подключён» больше не появляется.
- Код режима «Свой сервер» (`worker/index.js` + Anthropic) оставлен как запасной: чтобы включить, укажи `chatProvider: 'server'` и `chatEndpoint` в `config.js`.

## Сброс кэша при каждой публикации
- В `index.html` у всех `static/*.js|css` стоит `?v=dev`; шаг в `.github/workflows/pages.yml` при деплое заменяет его на короткий хеш коммита (и `window.WSPORT_BUILD`). Браузеры сразу получают свежие файлы, без «старой версии» из кэша.

## Обновление: КБЖУ + Сахар
- У каждого рецепта и коктейля есть поле `s` — сахар на порцию, г (`static/data.js`); у продуктов базы — 8-й элемент строки (`static/foods.js`).
- Сахар показывается рядом с Б/Ж/У/ккал: карточки рецептов, окно рецепта, «Готовый день» (у блюда и в итогах), поиск продуктов, выбор порции, штрих-код, распознавание по фото, ручной ввод (поле «Сахар, г»), строки дневника, заголовки приёмов пищи и сводка дня.
- Штрих-код и поиск OpenFoodFacts берут `nutriments.sugars_100g`; ИИ по фото возвращает `s` для каждого продукта. Если у товара нет данных о сахаре — пишется «—» / «нет данных», и в сводке отдельно считается «без данных: N».
- Ориентир в сводке: не больше 10% калорий нормы (рекомендация ВОЗ для добавленного сахара) = `норма ккал × 0,10 / 4` г. Если больше — плашка «Сахар» становится красной.
